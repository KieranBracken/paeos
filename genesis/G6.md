# G6 — Root Goal + Appetite Budget (FOUNDER ACT)

## Forcing clause
PAEOS-4 §10.2 (root budgets are founder-set) · §13.3 (self-hosting evidence spec) ·
§14.1 (founder ABI) · PAEOS-3 SVK-8 · package: **G-ROOT** (a founder task, not agent work).

## Writes
One `Goal(kind=work)` — the root of the entire goal tree:
- `spec.delta`: "PAEOS is self-hosting" (clauses c1–c4 per G-ROOT template, tagged per SC-05).
- `spec.evidence_spec`: the PAEOS-4 §13.3 self-hosting criterion, **verbatim** — editing
  it redefines self-hosting and is itself a constitutional change.
- `budget.allocated`: the founder's **appetite** — a bound, not an estimate (PAEOS-2 §1).
  All five dimensions > 0, including `decisions` (the founder's own attention; guidance:
  ≤ 12, and let exhaustion incidents calibrate it — that signal is the point).
- `constraints`: refs to K01–K11 plus any founder-global constraints (language, licensing —
  the L-04 judgment slot; silence here means escalations later).
- `parent: null`, `serving_clause: null`, `status: declared`.

## Inputs
`root-goal.json` — **not yet authored.** This is the founder's largest single act
(A4 intent). Recommended: run the A-15 root-goal ceremony (adversarial interrogation of
the delta for gaps) before committing.

## Exit evidence
Schema-valid root Goal admitted; evidence_spec verbatim-matches §13.3; budget vector
complete. Everything in Waves 1+ is registered as a descendant of this goal post-handoff.

## Note
This goal's own verification event is **M-RATIFY** (S4). The founder authors it at genesis
and does not touch it again except through the ABI — a direct edit afterward is an
auto-incident (clean-intent, PAEOS-2 §5).
