# G7 — Founder-ABI Refs

## Forcing clause
PAEOS-4 §14.1 (the sanctioned founder write surface) · PAEOS-2 §7 (five verbs) ·
PAEOS-3 SVK-9 (Decision queue + taste-corpus location, seeded).

## Writes
The reserved refs and namespaces that constitute the founder's entire interface to the
running system:
- **Decision queue** — the ref namespace where `Goal(kind=decision)` objects await founder
  `resolve`. Empty at genesis; fills as escalations occur.
- **Taste corpus namespace** — seeded with the exemplars mined from the Sentium excavation
  (G-CORPUS `corpus/taste/`), the ground truth Judges cite.
- (Inject and Author write through existing object paths; Audit is read-only — no new refs.)

## Inputs
`corpus/taste/` (G-CORPUS taste seeds) · the reserved ref-namespace conventions
(PAEOS-5.5 §05 storage model).

## Exit evidence
The five founder verbs (author, resolve, inject, curate, audit) each have a well-formed
target in the store; `cli` (backlog I-CLI) can operate all five. This completes the
Smallest Viable Kernel — the store is now a system the founder can drive and agents can
reconcile.

## After G7
Genesis (S0) is complete. The bootstrap proceeds by demand-paging (S1→S4): first agent,
Router, the delegation cascade, evolution ignition, and finally ratification — at which
point the genesis authority established by this whole sequence dissolves (PAEOS-3 §6).
Genesis runs exactly once; re-running on this non-empty store MUST fail (I-GEN).
