# Constitutional Review — B0.0 (Repo skeleton + toolchain + TCB-gated CI)

CER-4 / FP-3 mandatory close-out. Task: B0.0 (PAEOS-8 §10). Role: Builder. Date: 2026-07-22.

## Evidence (acceptance per PAEOS-8 §10 B0.0 row)
| Acceptance criterion | Result |
|---|---|
| `uv sync` clean (locked) | ✓ — `uv.lock` generated (201 lines), `--locked` sync clean; uv provisioned Python 3.13.5 (pin `>=3.12`) |
| `pytest` runs | ✓ — 1 skeleton-import test passes |
| ruff + pyright clean | ✓ — lint clean; pyright 0 errors, **strict on `kernel/`** via `strict=["kernel"]` |
| CI green on skeleton | ✓ — every `.github/workflows/ci.yml` step run locally and passes |
| LOC-budget gate (F1) RED-capable | ✓ — GREEN at 0/20000; **RED (exit 1)** at 25k dummy; GREEN after revert (the Verifier procedure, run locally) |
| TCB-diff gate (F2) works | ✓ — `kernel/` → HARD-LOOP (exit 2); `docs/` → SOFT (exit 0); fail-safe HARD on unknown |
| Supply-chain gate (F3) | ✓ — lockfile present; no committed key material; `ops/keys/` gitignored |

**Not satisfied:** the Day-1 spawnability spike (PAEOS-8 §13.2) — recorded as **DEBT-0002**
(HIGH, load-bearing; gates Phase-1 planning, not B0.1).

## The six questions (CER-4)

**(a) What architectural weaknesses were discovered?**
None in the constitution. Two *implementation* frictions: (1) ruff/pyright, run from repo root,
scan vendored `.agents/` skill tooling — fixed by `extend-exclude`; a real project needs a
declared source boundary, which the physical TCB layout (§1) already implies. (2) pyright's
per-environment `typeCheckingMode` is unrecognized in this build; the correct mechanism is the
top-level `strict` list — a documentation gap in PAEOS-8 §8 (it says "strict on kernel/" without
the exact pyright key). Minor.

**(b) What assumptions were invalidated?**
The language/tooling assumption I had left open in my own PAEOS-5 backlog (language = founder
decision) is **resolved**: PAEOS-8 §3 locks Python + uv + ruff + pyright + pytest + Postgres.
My backlog's G-ROOT "language constraint" slot is now answered by the founder's playbook. No
constitutional assumption was invalidated.

**(c) Which parts of PAEOS became stronger?**
The TCB boundary moved from prose to **executable**: the LOC-budget and TCB-diff gates now
mechanically enforce PAEOS-7.5 §3/A-2 — a `kernel/` change cannot merge on Builder+Verifier
approval alone. This is the first place the constitution *runs* rather than *describes*. The
CI is the constitutional CI, live from commit one.

**(d) Which Improvement Proposals were created?**
None. B0.0 revealed no constitutional improvement — only implementation execution. (CAP-1
remains the only open proposal, PAEOS-IP-0001.)

**(e) Which Architectural Debt items were recorded?**
**DEBT-0002** — the Day-1 spawnability spike, unexecuted (requires the target agent-runtime
environment; gates Phase-1 planning).

**(f) Should future implementation strategy change?**
No strategy change, one sequencing note: **do the spawnability spike (DEBT-0002) before
planning Phase 1**, exactly as PAEOS-8 §13.2 mandates. B0.1 (the ledger) does not depend on it
and is the correct next task. Also: the two doc tracks (my PAEOS-9A bootstrap vs the founder's
PAEOS-8/9-execution-architecture) agree on the build order — B0.0 → B0.1 (ledger) — so no
reconciliation is needed to proceed.

## Disposition
B0.0 build **complete and releasable** (FP-2: all gates green; working tree ready for the
founder's review + commit). One task this session (FP-1). Constitutional Review written (FP-3).
Passing evidence in place (FP-4). Independent review: N/A at bootstrap (humans play all roles,
PAEOS-8 §5) — the LOC/TCB gates are the mechanical verifier. **Next task: B0.1 — the
append-only, hash-chained, single-writer Ledger.**
