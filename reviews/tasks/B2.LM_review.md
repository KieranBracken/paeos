# Constitutional Implementation Review: [B2.L + B2.M — Objective Threading + Adversary Evidence]

**Date**: 2026-07-31
**Task**: Thread the goal objective into the stages (B2.L) and materialise the evidence bundle for
the Adversary (B2.M), so a live R4 run can build the real change and be genuinely reviewed.
**Reviewer Role**: Auditor / Builder Self-Adversarial

## Summary of Findings

- **BLOCKER**: 0 · **MAJOR**: 0 · **MINOR**: 0 · **OBSERVATION**: 1 (→ the profound live result)

Deliverables (`runtime/orchestrator/__init__.py`):
- **B2.L** — `SoftLoop.run` threads the intake **objective** into DESIGN/PLAN/IMPLEMENT (via
  `run_stage(detail=…)`); previously each stage got only a generic role objective and the Builder had
  nothing concrete to implement.
- **B2.M** — the Adversary's bundle carries **serialized, materialisable evidence** (`_serialize_
  evidence` → CAS), so the adversary can actually read and re-run the evidence (a bare hash resolved
  to nothing and it blocked "with nothing to attack").

Evidence: **313 passed** (+2); ruff clean (clean-cache); pyright 0 errors (strict on `kernel/`);
F1 **2644/20000** (unchanged — F2-SOFT: `runtime/` + `tests/`).

## The live result these unlocked (verified, contained R4 run)

With B2.L + B2.M (and B2.J/B2.K), the full pipeline ran genuinely for the first time:

1. **DESIGN + PLAN** produced real artifacts (B2.J).
2. **IMPLEMENT built the real change** (B2.L): the live Builder added a correct
   `classify_obj(obj) -> LifetimeClass` delegating to `classify_type(type(obj).__name__)`, exported
   in `__all__`. **classify_obj was actually written** (verified in the sealed artifact).
3. **ADVERSARY reviewed it** (B2.M gave it the evidence) and **broke the evidence, not the code**:
   > "It reproduces faithfully — but it's **non-probative**. The command is a tautology: it never
   > imports the module, never invokes `classify_obj`, contains no assertion… it returns the same PASS
   > for the claim and its negation… the same bare-`echo`-as-evidence shape as CANARY-0001. **VERDICT:
   > BLOCK**." (It even proposed a *new canary* for this defect class.)
4. **B2.K fail-closed** on the BLOCK → **REMANDED**. No hollow seal.

This is the architecture **working exactly as FR-3 promises**: the independent adversary caught an
*epistemic* defect (faithfully-reproducing-but-vacuous evidence) that the Court's deterministic check
alone could not — and refused the seal.

## Evaluation Checklist

### 1. Constitutional Compliance
**PASS.** B2.L only changes what the agent is *told to do* (the objective), not its authority. B2.M
only changes what the adversary can *read*. Both strengthen, never weaken, the gates. The REMAND on
vacuous evidence is FR-3/PAEOS-3.5 delivering its core value.

### 2–7. Drift / Duplication / Simplicity / Derivation / Debt
**PASS.** B2.L reuses the existing `run_stage(detail=…)` seam (the `detail` param already existed,
just never populated). B2.M reuses the CAS + the B2.J materialisation path. `_serialize_evidence` is
a plain JSON view. No new mechanism, no invention.

### 8. Security Implications
**PASS.** B2.M makes the adversary *more* capable (it can now re-run evidence) — strengthening
falsification. The live run is the proof: the adversary used it to catch a vacuous-evidence attack.

### 9–10. Runtime / Extensibility
**PASS.** Threaded objective + materialised evidence are the substrate a fully-live evidence flow
builds on.

## Adversary / property pass

1. **Objective threaded** — DESIGN/PLAN/IMPLEMENT packages each carry the goal objective (tested).
2. **Evidence materialisable** — the adversary's context carries an `evidence`-typed, CAS-resolvable
   ref; `_serialize_evidence` is readable (tested).
3. **Live proof** — the Builder built the real change; the Adversary reviewed and blocked on vacuous
   evidence; the loop remanded (contained run).

## OBSERVATION — the path to a *genuine* seal (the honest remaining gap)

The adversary is **right**: the R4 backlog's `reproducible_command: "echo r4-classify-obj"` is staged
and **non-probative** — it proves nothing about `classify_obj`. A genuine seal needs **probative
evidence**: a command that imports and exercises the change (`… assert classify_obj(...) is …`). But
the Court re-runs the command against a sandbox that does **not** have the builder's artifact applied,
so a real test cannot pass yet. Closing this — **the Court verifying the reproducible command against
the builder's produced artifact** (materialise the artifact, run the test against it) — is the real
live-evidence flow (runbook §5, toward R5). It is the next substantial task (**B2.N**), and it is the
*honest* reason Phase 2 is not yet sealed: not a machinery failure, but the adversary correctly
refusing a change whose evidence does not test it.

## Action Items
- [ ] **Founder**: ratify B2.L + B2.M (F2-SOFT) → merge ff-only, remote CI green, tag `b2.lm`.
- [ ] **B2.N** (next, for a genuine seal): the Court verifies the reproducible command **against the
      builder's artifact**, so probative evidence can pass — closing the vacuous-evidence gap the live
      adversary found. Capture the adversary's proposed canary for the vacuous-evidence defect class.
- [ ] Phase 2 seals when a real change passes a **probative** court check **and** the adversary PASSes.
