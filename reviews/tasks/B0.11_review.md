# Constitutional Implementation Review: [B0.11]

**Date**: 2026-07-27
**Task**: B0.11 — Change classifier stub / blast-radius SOFT-HARD (PAEOS-8 §10 / A-2)
**Reviewer Role**: Auditor / Builder Self-Adversarial (Adversary pass = T4 disguised TCB change)

## Summary of Findings

- **BLOCKER**: 0
- **MAJOR**: 0
- **MINOR**: 0
- **OBSERVATION**: 2

Deliverable: `kernel/classifier.py::classify_paths`. Evidence: 17 tests in
`tests/kernel/test_classifier.py`; 164 kernel tests total; ruff clean; pyright 0 errors (strict);
F1 2096/20000.

## Evaluation Checklist

### 1. Constitutional Compliance
**PASS.** Implements the A-2 rule verbatim: `kernel/` or `constitution/` ⇒ HARD; else SOFT;
unresolvable ⇒ HARD (fail-safe / deny-by-default). This is the classifier the gate's step-5 hook
(B0.6) consumes to route TCB changes to the amendment path.

### 2. Architectural Drift
**PASS.** A single pure function + normalisation helper. Stub scope honoured — full static
analysis is explicitly deferred to Phase 2 (playbook), not smuggled in.

### 3. Duplication of Mechanisms
**PASS with note.** Uses the same TCB prefixes as the CI F2 gate (`ops/ci/tcb_diff.py`). They are
intentionally parallel (kernel classifier vs CI script) and must agree — Observation 1.

### 4. Bypassing Mechanisms
**PASS.** Paths are normalised (`posixpath.normpath`, separator unification, leading `/`/`./`
stripped) *before* the prefix test, so the check cannot be bypassed by traversal or separator
tricks (§8).

### 5. Simpler Implementation Possible
**PASS / minimal.** No regex, no config; `posixpath` stdlib + a top-segment compare.

### 6. Better Derivation
**PASS.** Top-path-segment membership in `HARD_PREFIXES` is the most direct expression of "touches
kernel/ or constitution/", and avoids the `kernelish/` false-positive that a naive `startswith`
would produce (tested).

### 7. Hidden Technical Debt
**PASS.** None. The Phase-2 full-analysis upgrade is a playbook-scheduled successor, not debt.

### 8. Security Implications
**PASS — T4 closed.** Deny-by-default: empty change set, blank path, tree-escaping (`..`), and
traversal-into-TCB (`runtime/../kernel/x.py`) all classify **HARD** (tested). A genuine runtime file
whose name merely contains "kernel" (`runtime/kernel_shim.py`, `kernelish/x.py`) is correctly SOFT
— no over-classification that would route legitimate runtime work to the amendment path. Symlink
resolution is a filesystem concern beyond a path classifier (Observation 2).

### 9. Runtime Implications
**PASS.** Pure, O(paths), no I/O.

### 10. Future Extensibility
**PASS.** The Phase-2 analyzer replaces the body behind the same `classify_paths` signature; the
gate wiring (extracting changed paths from a request/proposal) is B0.12's composition step.

## Adversary pass (T4)

1. **Traversal into the TCB** (`runtime/../kernel/ledger.py`) → HARD (tested). **Double escape**
   (`docs/../../kernel/x.py`) → HARD via fail-safe (tested).
2. **Separator / prefix tricks** (`./kernel`, `/kernel`, `kernel\ledger.py`) → HARD (tested).
3. **False-positive probe** (`runtime/kernel_shim.py`, `kernelish/`) → SOFT — the classifier does
   not over-reach (tested).
4. **No-information** (empty set, blank) → HARD fail-safe (tested).

## OBSERVATIONS
1. **Parallel to F2.** `kernel/classifier.py` and `ops/ci/tcb_diff.py` encode the same TCB prefixes
   in two places (kernel runtime vs CI gate). They must stay in sync; a later task could share a
   single prefix constant. Kept separate now (CI script must run without importing the kernel).
2. **Symlinks out of scope.** Path classification catches traversal, not symlinked files; that is a
   filesystem/checkout concern for the control plane, not the classifier.

## Action Items & Proposals
- [ ] **Founder**: ratify B0.11 → merge, remote CI, tag `b0.11`.
- [ ] **B0.12 (CLI control plane)** next: wire `classify_paths` into the gate step-5 hook and the
      gate into the ledger append; `create-goal`, `advance --evidence`, `ledger`, `replay`, `seal`,
      `inspect`, capability-token acquisition.
