# Constitutional Implementation Review: [R5.3 — McpWorkerTransport + Court MCP Server]

**Date**: 2026-08-01
**Task**: Add the official MCP SDK behind `WorkerTransport` — a `CourtServer` writing into
`FileCourtEvidencePool`, with **zero core-runtime change** (Phase 3 / R5).
**Reviewer Role**: Auditor / Builder Self-Adversarial

## Summary of Findings

- **BLOCKER**: 0 · **MAJOR**: 0 · **MINOR**: 0 · **OBSERVATION**: 2

Deliverables:
- **MCP SDK dependency** added (F3 gate; `pyproject.toml` + `uv.lock` +525). `import mcp` now resolves
  to the SDK — the R5.1 namespace fix proven.
- `runtime/transports/mcp/court_server.py` — the Court MCP server (`MCPServer`) exposing a
  `submit_evidence` tool that writes into a durable `FileCourtEvidencePool`. Stdio `main` for
  `claude --mcp-config`. **The only module that imports the MCP SDK** — the vendor boundary.
- `runtime/transports/mcp/transport.py` — `McpWorkerTransport`: a pool-reading `WorkerTransport` +
  `EvidenceSource` + `mcp_config()` (the `--mcp-config` that launches the server against the pool).

Evidence: **330 passed** (+3, incl. the real MCP tool driven in-process via `MCPServer.call_tool`);
ruff clean (clean-cache); pyright 0 errors (strict on `kernel/`); F1 **2644/20000** (unchanged).

## What this delivers — and the invariant it proves

The third transport completes the `WorkerTransport` trilogy (File / ClaudeCli / Mcp). A worker submits
evidence over MCP → the court server writes it to the pool → the `SoftLoop` reads it via
`McpWorkerTransport` (an `EvidenceSource`) → seals. Tested end-to-end in-process: the `submit_evidence`
tool runs, and the loop **seals on MCP-submitted evidence** with no pre-declared backlog.

**The CER-1 / R5.1 payoff, realised:** adding a whole vendor SDK + a new transport touched **zero
core-runtime code** — verified: `runtime/` imports no MCP except under `runtime/transports/mcp/`.

## Evaluation Checklist

### 1. Constitutional Compliance
**PASS.** The worker submits; evidence is inert until the kernel adjudicates (SI-2). MCP-submitted
evidence faces the full gauntlet (B2.I/B2.N/B2.O/B2.K) unchanged. `ledger.append` remains unexposed.

### 2. Architectural Drift — and an AI-001 refinement
**PASS.** The MCP SDK is imported **only** under `runtime/transports/mcp/` — the sanctioned vendor
boundary. This **refines AI-001** (IP-0008): the *verifier* is `grep "import mcp"` over `runtime/`
**excluding `runtime/transports/`** — the transports layer is precisely where vendor imports are
allowed; the core must stay clean (it does).

### 3–7. Duplication / Simplicity / Derivation / Debt
**PASS.** `McpWorkerTransport` is a thin pool reader + config generator; the server is a single tool.
No duplication of the pool. The SDK dependency is now *used* (not speculative), satisfying F3
minimality.

### 8. Security Implications
**PASS.** Vendor isolation is the security property: the MCP SDK (starlette/uvicorn/etc.) sits behind
the transport boundary; a compromise cannot reach the core runtime. `uv.lock` pins the supply chain
(F3); no keys committed.

### 9–10. Runtime / Extensibility
**PASS.** Any future transport (a queue, a socket) is another `WorkerTransport` behind the same
boundary — zero core change, now demonstrated three times.

## Adversary / property pass

1. **Real MCP tool** — `build_court_mcp_server().call_tool("submit_evidence", …)` writes to the pool;
   `McpWorkerTransport.receive` returns it (tested).
2. **Autonomous seal** — the `SoftLoop` seals on evidence submitted over MCP (tested).
3. **Config** — `mcp_config()` launches `runtime.transports.mcp.court_server` against the pool (tested).
4. **Core untouched** — only `runtime/transports/mcp/` imports the SDK (grep).

## OBSERVATIONS

1. **The live stdio round-trip is deployment.** Tests drive the tool in-process (`call_tool`);
   standing up the server over real stdio and connecting a live `claude --mcp-config` to it (and
   iterating a fully-autonomous run, like the R4 seal) is the deploy step. The server, tool, transport,
   config, and autonomous seal-on-MCP-evidence are all proven.
2. **AI-001's CI verifier needs the `transports/` exclusion** (above) when IP-0008's registry is built.

## Action Items
- [ ] **Founder**: ratify R5.3 (F2-SOFT + F3 dependency) → merge ff-only, remote CI green, tag `r5.3`.
- [ ] **R5 deployment**: run a live fully-autonomous self-hosting run — `claude --mcp-config` → court
      server → pool → seal (no pre-declared evidence).
- [ ] When IP-0008 lands: encode AI-001 with the `runtime/transports/` exclusion.
